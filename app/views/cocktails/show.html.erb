
<div class="cocktail-container">


  <div class="cocktail-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path @cocktail.photo.key %>)">    
    <h1><%= @cocktail.name %></h1>
  </div>

  
    <div class="sub-title">
      <p>INGREDIENTS</p>
    </div>
  
  <% @cocktail.doses.each do |dose| %>    
    <div class="ingredients-box">
      <p><%= dose.description %> of <%= dose.ingredient.name %></p>
      <%= link_to dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } do%>
        <i class="far fa-trash-alt"></i>
      <%end %>
    </div>
  <% end %>

  <%= render 'doses/new' %>
    
</div>

